import { AlphabetsDirective } from './alphabets.directive';

describe('AlphabetsDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetsDirective();
    expect(directive).toBeTruthy();
  });
});
