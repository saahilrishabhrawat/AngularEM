import { Alphaspec3Directive } from './alphaspec3.directive';

describe('Alphaspec3Directive', () => {
  it('should create an instance', () => {
    const directive = new Alphaspec3Directive();
    expect(directive).toBeTruthy();
  });
});
