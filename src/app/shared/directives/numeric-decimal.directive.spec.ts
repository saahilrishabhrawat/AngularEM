import { NumericDecimalDirective } from './numeric-decimal.directive';

describe('NumericDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new NumericDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
