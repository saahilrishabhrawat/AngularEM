import { AlphanumallspecDirective } from './alphanumallspec.directive';

describe('AlphanumallspecDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumallspecDirective();
    expect(directive).toBeTruthy();
  });
});
