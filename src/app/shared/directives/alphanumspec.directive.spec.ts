import { AlphanumspecDirective } from './alphanumspec.directive';

describe('AlphanumspecDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumspecDirective();
    expect(directive).toBeTruthy();
  });
});
