import { DatepickerMmddyyyyDirective } from './datepicker-mmddyyyy.directive';

describe('DatepickerMmddyyyyDirective', () => {
  it('should create an instance', () => {
    const directive = new DatepickerMmddyyyyDirective();
    expect(directive).toBeTruthy();
  });
});
