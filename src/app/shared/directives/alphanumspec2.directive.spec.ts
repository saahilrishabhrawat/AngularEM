import { Alphanumspec2Directive } from './alphanumspec2.directive';

describe('Alphanumspec2Directive', () => {
  it('should create an instance', () => {
    const directive = new Alphanumspec2Directive();
    expect(directive).toBeTruthy();
  });
});
