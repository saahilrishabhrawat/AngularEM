import { AlphaspecDirective } from './alphaspec.directive';

describe('AlphaspecDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaspecDirective();
    expect(directive).toBeTruthy();
  });
});
