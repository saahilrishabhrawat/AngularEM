import { Alphaspec2Directive } from './alphaspec2.directive';

describe('Alphaspec2Directive', () => {
  it('should create an instance', () => {
    const directive = new Alphaspec2Directive();
    expect(directive).toBeTruthy();
  });
});
