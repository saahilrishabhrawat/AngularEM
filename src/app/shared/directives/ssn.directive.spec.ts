import { SsnDirective } from './ssn.directive';

describe('SsnDirective', () => {
  it('should create an instance', () => {
    const directive = new SsnDirective();
    expect(directive).toBeTruthy();
  });
});
