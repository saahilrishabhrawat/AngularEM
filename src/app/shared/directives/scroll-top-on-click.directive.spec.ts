import { ScrollTopOnClickDirective } from './scroll-top-on-click.directive';

describe('ScrollTopOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTopOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
