import { CenturyToDatePipe } from './century-to-date.pipe';

describe('CenturyToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CenturyToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
